server:
  error:
    include-stacktrace: on-param


spring:
  datasource:
    url: jdbc:mysql://localhost:3307/animais?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: mysql
    password: senha123
  jpa:
    hibernate:
      ddl-auto: update